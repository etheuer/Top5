%h1= @list.title
%p= @list.description

%p 
Submitted by 
= @list.user.email

%br/
= link_to "Back", root_path
= link_to "Edit", edit_list_path
= link_to "Delete", list_path, method: :delete, data:  { confirm: "Are you sure?"}